# 📦 GUI System - Installation Instructions

## ✅ Что создано:

### Новые файлы (создать):
1. **PortGUI.java** → `src/main/java/com/example/boatroutes/gui/PortGUI.java`
2. **GUIUtils.java** → `src/main/java/com/example/boatroutes/gui/GUIUtils.java`
3. **GUIListener.java** → `src/main/java/com/example/boatroutes/gui/GUIListener.java`
4. **BoatSpawner.java** → `src/main/java/com/example/boatroutes/boat/BoatSpawner.java`

### Файлы для замены (обновлённые):
5. **BoatManager_new.java** → переименовать в `BoatManager.java` и заменить старый
6. **PlayerListener_new.java** → переименовать в `PlayerListener.java` и заменить старый
7. **NPCListener_new.java** → переименовать в `NPCListener.java` и заменить старый
8. **GUIManager_new.java** → переименовать в `GUIManager.java` и заменить старый
9. **BoatRoutesPlugin_new.java** → переименовать в `BoatRoutesPlugin.java` и заменить старый

---

## 📝 Пошаговая установка в IntelliJ IDEA:

### Шаг 1: Создай новые файлы

1. **PortGUI.java**
   - Правый клик на папку `gui/`
   - New → Java Class → `PortGUI`
   - Скопируй содержимое из скачанного файла

2. **GUIUtils.java**
   - Правый клик на папку `gui/`
   - New → Java Class → `GUIUtils`
   - Скопируй содержимое

3. **GUIListener.java**
   - Правый клик на папку `gui/`
   - New → Java Class → `GUIListener`
   - Скопируй содержимое

4. **BoatSpawner.java**
   - Правый клик на папку `boat/`
   - New → Java Class → `BoatSpawner`
   - Скопируй содержимое

---

### Шаг 2: Замени обновлённые файлы

1. **BoatManager.java**
   - Открой `src/main/java/com/example/boatroutes/boat/BoatManager.java`
   - Выдели весь текст (Ctrl+A)
   - Удали (Delete)
   - Скопируй содержимое из `BoatManager_new.java`
   - Вставь (Ctrl+V)

2. **PlayerListener.java**
   - Открой `src/main/java/com/example/boatroutes/listeners/PlayerListener.java`
   - Выдели весь текст (Ctrl+A)
   - Удали
   - Скопируй из `PlayerListener_new.java`
   - Вставь

3. **NPCListener.java**
   - Открой `src/main/java/com/example/boatroutes/listeners/NPCListener.java`
   - Выдели весь текст (Ctrl+A)
   - Удали
   - Скопируй из `NPCListener_new.java`
   - Вставь

4. **GUIManager.java**
   - Открой `src/main/java/com/example/boatroutes/gui/GUIManager.java`
   - Выдели весь текст (Ctrl+A)
   - Удали
   - Скопируй из `GUIManager_new.java`
   - Вставь

5. **BoatRoutesPlugin.java**
   - Открой `src/main/java/com/example/boatroutes/BoatRoutesPlugin.java`
   - Выдели весь текст (Ctrl+A)
   - Удали
   - Скопируй из `BoatRoutesPlugin_new.java`
   - Вставь

---

### Шаг 3: Проверь структуру

После установки структура должна быть:

```
src/main/java/com/example/boatroutes/
├── BoatRoutesPlugin.java        ← ОБНОВЛЁН
├── boat/
│   ├── BoatManager.java         ← ОБНОВЛЁН
│   ├── BoatSpawner.java         ← НОВЫЙ
│   └── PlayerBoat.java
├── gui/
│   ├── GUIManager.java          ← ОБНОВЛЁН
│   ├── PortGUI.java             ← НОВЫЙ
│   ├── GUIUtils.java            ← НОВЫЙ
│   └── GUIListener.java         ← НОВЫЙ
└── listeners/
    ├── NPCListener.java         ← ОБНОВЛЁН
    ├── PlayerListener.java      ← ОБНОВЛЁН
    └── VehicleListener.java
```

---

## 🔨 Шаг 4: Собери проект

```bash
./gradlew clean build
```

Если есть ошибки компиляции - сообщи мне!

---

## 🎮 Тестирование:

### Тест 1: Создание порта
```
/port create testport
```

### Тест 2: Установка NPC
- ПКМ по земле с яйцом

### Тест 3: Установка причалов
- ПКМ по воде 3 раза

### Тест 4: Открытие GUI
- ПКМ по NPC
- Должно открыться меню с кнопками

### Тест 5: Создание лодки
- Клик на "Create New Boat"
- Напиши имя в чат (например: "MyBoat")
- Лодка должна появиться у причала

### Тест 6: Проверка лодки
- Лодка должна иметь название "🚤 MyBoat"
- Лодка должна быть нерушимой (Invulnerable)
- Лодка должна быть видна в GUI как "My Boat"

### Тест 7: Удаление лодки
- Открой GUI снова
- Клик на "My Boat"
- Лодка должна исчезнуть

---

## ✨ Что теперь работает:

✅ Открытие GUI при клике на NPC  
✅ Красивое меню с кнопками  
✅ Ввод имени лодки через чат  
✅ Создание лодки с кастомным именем  
✅ Лодка нерушимая (invulnerable)  
✅ Лодка с видимым названием  
✅ Отслеживание одной лодки на игрока  
✅ Удаление лодки через GUI  
✅ Информация о порте в GUI  

---

## ❌ Что ещё не работает:

- Навигация между портами (кнопка есть, но функция в следующем этапе)
- Сохранение лодок в файл (пока только в памяти)
- Автопилот (следующий этап)

---

## 🐛 Если что-то не работает:

1. **Ошибка компиляции** → проверь что все файлы в правильных папках
2. **GUI не открывается** → проверь что GUIListener зарегистрирован в BoatRoutesPlugin
3. **Лодка не создаётся** → проверь что есть свободный причал
4. **Имя не принимается** → используй только английские буквы/цифры

---

## 📝 Следующий этап:

После тестирования можем перейти к:
- **Pathfinding** (A* алгоритм) - для прокладки маршрутов
- **Navigation GUI** - выбор направления
- **Autopilot** - автоматическое движение лодки

---

**Успехов с тестированием!** 🎉
